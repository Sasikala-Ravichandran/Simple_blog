<div class="sub_header">
    <%= link_to "Back", user_post_path(@post.user, @post), :class => "right buttons"%>  
</div>

<div class="logged_posts_wrapper">
<%= form_for([@post, @comment]) do |f| %>
	 
	<%= f.label :body, "Your comments" %>:<br>
	<%= f.text_area :body %><br>
	<br>
	<%= f.submit %>
<% end %>
</div>