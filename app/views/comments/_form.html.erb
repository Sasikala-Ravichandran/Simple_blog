<%= form_for([@post, @post.comments.build], remote: true) do |f| %>
  
  
	<%= f.text_area :body, rows: 2, cols: 60, placeholder: "Write a comment..." %><br>
	<br>
	<%= f.submit "Post comment"%>
 
 
<% end %>