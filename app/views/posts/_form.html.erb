<%= form_for([@user, @post]) do |f| %>

  <%= f.label :title %>:<br>
  <%= f.text_field :title %><br><br>

  <%= f.label :body %>:<br>
  <%= f.text_area :body %><br><br>

  <%= f.label :tag_list %>:<br>
  <%= f.text_field :tag_list %><br><br>
  
  <%= f.submit %>
<% end %>