<%= form_for([@user, @post]) do |f| %>

  <%= f.label :title %>:<br>
  <%= f.text_field :title %><br>

  <%= f.label :body %>:<br>
  <%= f.text_area :body %><br>

  <%= f.label :tag_list %>:<br>
  <%= f.text_field :tag_list %><br>
  
  <%= f.submit %>
<% end %>