<%= link_to "Back to Dashboard", current_user%><br>
<hr>
Title: <%= @post.title %><br>

Content: <%= @post.body %><br>

<%= link_to "Edit", edit_user_post_path(current_user.id, @post)%>
<%= link_to "Delete", user_post_path(current_user.id, @post), method: :delete, 
									data: {confirm: "Delete the post?"}%>


 <hr>
Comments:
<hr>
<% @post.comments.each do |comment| %>
    <%= link_to comment.commenter.name unless comment.user_id.nil? %><br>
    <%= comment.body %><br>
    <%= time_ago_in_words(comment.created_at) %><br>
    <%= link_to "Delete", post_comment_path(@post, comment), method: :delete %>
    <hr>
<% end %>


<hr>
Add Comments

<%= render 'comments/form'%>